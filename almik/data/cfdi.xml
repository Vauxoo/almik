<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <template id="cfdiv40" inherit_id="l10n_mx_edi_40.cfdiv40">
        <xpath expr="//*[local-name()='Concepto']" position="attributes">
            <attribute name="t-att-ValorUnitario">
                format_float(
                    (
                        line_values["price_subtotal_before_discount"]
                        if tax_objected == "02"
                        else (line.price_total + line_values["price_discount"])
                    ) / line.quantity,
                    6,
                )
            </attribute>
        </xpath>
    </template>
</odoo>
